# requirements.txt — WEB image only (the read-only Flask frontend).
# Heavy batch/ML deps live in requirements-batch.txt so this image stays small
# and never pulls torch. Web reads story_cards from the DB and renders them.

flask>=3.0
gunicorn>=21.0
psycopg[binary]>=3.1
python-dotenv>=1.0
